todo
1) reorganize documentation
2) full docstrings for functions
3) add aberrations

# consider regenerating images using docs/images/readme_images.ipynb
  make readme
  git commit docs/images/*png -m 'updates to readme images'

# sanity check
  make rcheck

# edit version in __version__.py
  git commit -m 'update version' __version__.py

# update CHANGELOG.txt  `git shortlog 1.0.0..HEAD`
  git commit -m 'update recent changes' CHANGELOG.txt
  git push

# update jupyterlite pages on github
  make lite-clean
  make lite
  make lite-deploy

# create release tag on github
  git tag 1.0.0
  git push origin 1.0.0

# publish release
  open https://github.com/scottprahl/lenstest

# check release
  open https://lenstest.readthedocs.io
  open https://pypi.org/project/lenstest/
  open https://pypi.org/project/lenstest/

# wait for conda release